{
  "name": "stacks-vault-and-escrow",
  "private": true,
  "packageManager": "yarn@4.6.0",
  "workspaces": [
    "vault",
    "escrow"
  ],
  "scripts": {
    "format": "prettier -w .",
    "format:check": "prettier -c .",
    "lint": "eslint . && yarn workspace vault lint",
    "typecheck": "yarn workspace vault typecheck",
    "build": "yarn workspace vault build"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.7.4",
    "typescript": "^5.9.3"
  }
}
